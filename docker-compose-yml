init:
  image: svenahlfeld/rediscluster
  link:
  - redis
  labels:
     io.rancher.container.start_once: true
  command: /redis/bootstrap.sh ${redis_port} ${rep_factor} init
  
redis:
  image: svenahlfeld/rediscluster 
  command: /redis/bootstrap.sh ${redis_port} ${rep_factor} client
